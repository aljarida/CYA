[tool.pytest.ini_options]
markers = ["llm: requires a real OpenAI API key; skipped by default, run with --llm"]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]
